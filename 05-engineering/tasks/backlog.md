# Engineering Task Backlog

> Generated by `/build` (Engineering Director). Update status as tasks move.
> Run `/build [task-name]` to implement any task.

<!-- This file is populated by the Engineering Director after `make scaffold`.
     Run `/build` with no arguments to generate the initial backlog from spec.md. -->
