Read `.claude/guides/agents/orchestrator.md` to understand the routing protocol.
Then read `.claude/guides/interview.md` for the interview methodology.

Check that `pitch.md` exists and contains content beyond the placeholder comment. If not, stop and tell the user to fill it in first.

Check `CLAUDE.md` for the current phase — if we're past 00-discovery, resume from the current phase rather than starting over.

Begin.
